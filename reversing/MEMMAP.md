# Metal Slader Glory (FC) 内存/寄存器映射表

像手搓时代那样,逐字节标注用途。实机(Mesen)观测 + 反汇编(`dis6502.py` + CDL)得来。持续补充。

## RAM(零页 + $02xx-$07xx)

| 地址 | 用途 | 来源/备注 |
|---|---|---|
| `$01` | PPUMASK 影子(NMI 写 $2001) | 反汇编 NMI |
| `$17` | **自增帧计数器**,一有输入被接受就归 01(非"就绪标志") | 菜单探针 |
| `$2C` | **命令菜单光标**(0-based;子菜单也从0起);down/up 改它,A 选中 | 菜单探针 |
| `$87/$88` | **当前句子指针**(lo/hi);$F071 处执行时据此算句号 | scenetrace |
| `$9D` | 帧计时器(每帧变) | scenetrace wait_ready |
| `$B3/$B4/$B5` | NMI 保存的 A/Y/X | 反汇编 NMI($E9C3) |
| `$0200` | OAM[0].Y;**翻页箭头▽** 显示时 =`$F0`(对话等待按A) | 自动游玩 |
| `$0201` | OAM[0].tile | |
| `$0450` | **当前背景CG bank / 场景号**(0A序幕/30机甲/67通电/0B海边/10店门口/11店内/17查米近景/0E工作间/12/13/14阿源车/16穿梭机/00密语系统屏) | 全程核心 |
| `$045E` | region0(上半CG)bank;NMI 从 $0450 拷入,IRQ 应用到 $512B | 反汇编+notes |
| `$045F` | **region1(对话框)字库 bank**。原版 NMI:`=$0451`(=0);汉化改成 `=$0450\|0x80`(每场景独立字库) | probe/反汇编 |
| `$0451` | **原版对话字库 bank 号 = 0**(即 CHR bank0 = 原版假名字库) | probe 原版ROM |
| `$0454` | 立绘/头像 bank(经指针链) | notes |
| `$0461` | region1 sprite bank(IRQ 应用到 $5127) | notes |

## MMC5 寄存器($5xxx)

| 地址 | 用途 |
|---|---|
| `$5100/$5101` | PRG/CHR 模式 |
| `$5104` | ExRAM 模式(0=名字表/1=扩展属性——**可每tile选CHR bank,全量翻译的潜在钥匙**) |
| `$5113-$5117` | PRG bank($5117=$FF→末bank映射$E000;复位向量在此) |
| `$5120-$5127` | **sprite CHR bank**(8×1KB)。CONTINUE 界面写 $5127=00 |
| `$5128-$512B` | **bg CHR bank**(4×2KB)。CONTINUE 界面写 $512B=00 |
| `$5130` | CHR bank 高位(扩展 CHR>256bank 用) |
| `$5203/$5204` | 扫描线 IRQ 比较值/状态(分屏对话框靠它) |
| `$5205/$5206` | 硬件乘法器 |

## 分屏渲染机制(2026-07-08 反汇编+实机确认)★核心

屏幕竖分多区(region),每区一套 CHR bank。区 bank 存 shadow 数组 **$045E-$0461**(=region0/1/2/3):
- NMI(每帧)从 $0450-$0453 拷入 $045E-$0461:`$EB79 LDA$0450;STA$045E` / `$EB7F LDA$0451;STA$045F`(汉化`ORA#$80`) / `$EB85 LDA$0452;STA$0460` / `$EB8B LDA$0453;STA$0461`。
- **NMI $EB58 `LDA $0450,X; STA $512B/$5123`** 设**顶部 strip** bank:X=0→$0450(=CG),X=2→$0452。X 由 $EBC7,Y 表 + $044E 决定。
- 分屏 IRQ($F978→`JMP($0633)`动态派发)+ **$FCC9/$FF34 `LDA $045E,Y; STA $512B/$5123`** 按区号 Y 应用各区 bank。
- 实测:普通场景 2 区(region0=CG=$045E / region1=对话框=$045F);**场景67 三段**:顶部绿字 strip(=$0452)+中间CG portrait(region0=$045E=67)+底部对话框(region1=$045F=E7)。
- **region2/3($0460/$0461)全程 =0/未渲染**(attract/海边/穿梭机实测);绿字顶部 strip 走 $EB58 X=2→$0452,**非** $0460。

## CHR 布局(汉化 build,扩到 1MB)

| bank | 内容 |
|---|---|
| `0`(CHR0=file 0x100010) | **原版满字库(253码非空,仅00/01/FF空)**。★保持纯净不污染→场景00 密语/存档屏用它渲染假名 |
| `$80+场景`(如$8B海边/$94阿源车) | 汉化每场景独立字库(bank0拷贝+中文注入) |
| `$E0`(GREEN_BANK) | **绿字专用**=bank0完整拷贝+句63中文(仅~12字)。NMI $EB85 钩子把 $0452→0xE0,场景67 顶部读它 |
| `$A0+场景` | **空闲**(多bank架构可用作第二字库bank) |

## 关键代码地址(CPU)

| 地址 | 用途 |
|---|---|
| `$E9C3` | NMI 入口(向量 $FFFA) |
| `$EB58` | NMI 顶部 strip bank:`LDA $0450,X; STA $512B/$5123`(X=2→$0452=绿字) |
| `$EB7F`(file 0x7EB8F) | **region1 字库钩子**:原`LDA$0451;STA$045F`→汉化`ORA#$80;STA$045F;NOP`(对话框=$0450\|0x80) |
| `$EB85`(file 0x7EB95) | **region2 钩子(绿字修复)**:原`LDA$0452;STA$0460`→汉化`LDA#$E0;STA$0452;NOP`(每帧置$0452=GREEN_BANK) |
| `$F071` | 句子处理点(据 $87/$88 算句号) |
| `$F978` | IRQ 入口(向量 $FFFE)→分屏引擎(`JMP($0633)`动态派发,句柄表$FCA8/$FCAE) |
| `$FCC9/$FF34` | 分屏应用区 bank:`LDA $045E,Y; STA $512B/$5123`(多bank架构②的钩子点) |

## 密语/CONTINUE 机制(2026-07-08 完全确认+已修)

- **密语 = "えりな もう おきなよ"**(码 え53 り77 な64 も72 う52 お54 き56 よ75),句154(场景14 艾莉娜口述)教给玩家。
- **CONTINUE 屏**(标题选 CONTINUE,场景00):顶部忠回忆独白(**句22** "‥‥ あー よく寝た‥‥エリナ…おこしてくれって…")+ 中间蓝框密语输入 + 底部**全假名选择格**(あ-ん/ア-ン/浊音/符号/ススム前进/オワル)。全用 **bank0** 渲染。
- **存档屏**(场景14 选休息→场景00):句15/17/19/20。
- **乱码根因(已修)**:①build 把整场景67(~104字)灌进 bank0→密语屏假名被中文顶掉;②句22 等系统句不改写,但其引用的 block 被 block_for 当"空闲"重分配给中文→原版假名 block 被顶掉。
- **修复(build_ch1_mb.py)**:①bank0 不再注入绿字(绿字改用 GREEN_BANK,见上);②场景00 句(gui_scene==0)不改写;③SYS_SENTENCES(22/15/17/19/20/…)的 block 全部 RESERVED;④KANA2CODE:译文里的原版假名走原始 tile 码(码从 CODE_POOL 保留)→句154 密语保假名 "えりな もう おきなよ",玩家照假名形输入即可。
- 实测:CONTINUE 屏假名格+独白与原版逐字节一致;句154 密语显示假名;绿字中文;普通场景顶部CG完好。

## 待办
1. ~~密语修复~~ ✓ 已完成(2026-07-08)
2. **多bank架构**:改 $FCC9/$FF34 或 $EB7F,按当前句($87/$88)选 A bank($80+场景)/B bank($A0+场景)→热点场景(海边296/查米近景277字>190)全量翻译。
3. 回访/深话题句乱码(148/186/216 等):句在非其字库场景显示→字库对不上。根治靠②多bank或按显示场景重复打包。
4. bank62 打字机(可选真16×16)
