# Metal Slader Glory — 文本引擎逆向笔记

面向中文化的静态逆向成果。地址约定：`PRG` = PRG ROM 内偏移（= .nes 文件偏移 − 0x10）；`CPU` = 6502 地址。
标注**确认**（从指令直接读出/与 `msgtool.py` 交叉验证）或**推测**。

## 卡带 / MMC5 配置（确认）

- Mapper 5 (MMC5)，PRG 512KB + CHR 512KB，带电池 SRAM，水平镜像。
- 模式寄存器（reset 于 `$E94E` 设置）：`$5100=3`（PRG 8KB×4）、`$5101=1`（**CHR 4KB banking**）、`$5104=2`（ExRAM 作普通工作 RAM）、`$5105` nametable 映射。
- PRG 窗口：`$E000-$FFFF` = `$5117` = **固定 bank 63**；`$A000-$BFFF` = `$5115` = 文本解引用时按需切换的数据窗口；`$8000`/`$C000` 也动态切。
- 向量：`NMI=$E9C3  RESET=$E94E  IRQ=$F978`（均在 bank 63）。
- **可执行代码只在 bank 59–63（PRG `0x76000–0x7FFFF`，顶部 40KB）**，其余 472KB 全是数据（CDL code 标记佐证）。

## tile 映射（确认：恒等）

**PPU 编码字节 b → 字库 tile 号 = b，无偏移、无查表。** 逐环验证：ROM 的 PPU 串字节 → 行缓冲 `$0469` → 字符对缓冲 `$04A9,X`（`$F176: STA $04A9,X`，前无算术）→ VRAM 队列 `$0300`（直拷）→ `$2007`（`$F8F6: LDA $0300,X; STA $2007`）。
字库 = 一个 CHR 4KB bank（256 tile），tile 排列 = `msgtool.py` 的 `CHARSET` 顺序（渲染验证：tile 0x20='0'、0x2A='A'、0x50='あ'、0x0E='゛'、0xFE=空白）。

## 屏上字符几何（确认）

每个对话字符 = **上下两个 8×8 tile 竖排**（cell 8×16，字形在下格）：
- 渲染为每字生成一对 `[mark, char]` 存入 `$04A8`（偶=上格 tile，奇=下格 tile）。
- `char` = 字形 tile（恒等）；`mark` 默认 `0xFE`（空白），仅当 PPU 串遇 `0x0E`/`0x0F` 前缀时置为 `゛`/`゜`。
- **浊音/半浊音 = 把 ゛/゜ tile 摆到假名上方那格**（硬件叠放，非合成字形）。

## 例程地图（确认）

| 例程 | CPU / PRG | 作用 |
|---|---|---|
| 文本渲染核心 | `$F0B7` / `0x7F0B7`（备用入口 `$F0C0`） | 句块串 → 块 → PPU 串 → `$04A8` 对缓冲 |
| level-1 指针解引 | `$EF96` / `0x7EF96` | 从 `$29/$2A` 取块串指针 |
| block→文本指针 | `$EFB9` / `0x7EFB9` | 内部调 `$F01D` |
| ×3+基址+切bank | `$F01D` / `0x7F01D` | text_pointer；用 ExRAM `$5D1B/$5D1E/$5D21` 基址表，切 `$5115` |
| **string_pointer 解包** | `$EFC3` / `0x7EFC3` | **与 msgtool `string_pointer` 逐位一致**：`bank=b1&0x3F→$5115`、`ptrHi=(b3&0x1F)+0xA0`、`len=((b1>>6)<<3)+(b3>>5)` |
| 拷 PPU 串到行缓冲 | `$EFEB` / `0x7EFEB` | ROM `($81),Y` → `$0469,X`；首字节 0 的特例同 msgtool |
| 字符发射循环 | `$F135–$F185` | `$0469` PPU 字节 → `$04A8` `[mark,char]` 对；`$27` 锁存浊音前缀 |
| 矩形缓冲→VRAM队列 | `$E270` / `0x7E270` | `$04A8` → `$0300` 队列（参数块 `$04A3..`：宽、行数=2、PPU 地址、flags） |
| VRAM 队列刷新 | `$F8AA` / `0x7F8AA` | vblank 内把 `$0300` 队列写 `$2007`，预算 `$0449=#$4C`（76B/帧）；包格式 `[count][addrHi][addrLo][data…]` |
| 行排版/分页 | `$A4D7`（bank61 `0x7A4D7`） | 走块串、处理控制码、按 `#$1E`(30 tile) 宽度断行、建行指针表 `$04E8`（≤8 段） |
| NMI | `$E9C3` | OAM DMA；调色板上传；MMC5 行 IRQ 设置（`$5203/$5204`）；载入 CHR bank（`$EB5B`: `$0450→$512B/$5123`, `$0453→$5127`） |
| IRQ（光栅分屏） | `$F978` | 经 `($0633)` 分派；分屏应用器 `$FF26` 从 `$045E,Y→$512B/$5123` 逐区切 CHR/滚动 |

### 渲染核心伪代码
```
render_line($F0B7):                     # $29/$2A=行块串指针, A=起始列*2
  $27 = 0xFE
  ($18,$19) = resolve_ptr($29/$2A)      # $EF96
  loop over block-string ($18),Y:       # $F0D7
     b = read()
     if b==0x00: finalize()
     elif b>=0x80: block=(b<<8)|next    # 双字节块(MTE)
     else:         block=b
     expand_block(block) -> $0469[]     # $EFB9: block→text→string→拷PPU串
     for each PPU byte c in $0469:       # $F135..$F185
        if c in {0x0E,0x0F}: $27=c; continue   # 锁存浊音到上格
        $04A8[k]   = $27 ; $27 = 0xFE          # 上格 = 标记/空白
        $04A9[k+1] = c                         # 下格 = 字形 tile（恒等）
        k += 2
  # $E270 把 $04A8 作 2 行 blit 进 $0300 队列 → NMI 刷 $2007
```

## CHR bank（确认）

- CHR 4KB 模式：512KB = **128 个 4KB bank，每 bank 256 tile**。
- 文字在背景层，字库 bank = 写 `$512B` 的值；分屏表 `$045B-$0468`（NMI 从 `$0450-$045A` 拷来），IRQ 应用器 `$FF34` 写 `$512B`。复位默认字库 = bank 0。
- ⚠ **修正（PoC 实测）**：先前"对话框区独占 256-tile 页"的说法**不成立**。开场机甲幕 / 海边幕的 CG 与对话字库**共享同一个 bank**，且该 bank 几乎全满（纯空白 tile 仅 1–3 个）——tile `0xB0–0xC5` 在 bank 0/11/48 里都是 CG 图形块（`ink=64` 满格）。**这些场景没有空位无损植入新字模**，详见下方 PoC 坑 3。

## 控制码（推测）

块串层控制码（05/07/0B/0C/13/14/16/18 双字节；0F/10/12/17 三字节）**不在**字形渲染例程分派——核心 `$F0D7` 只认 `0x00`/`≥0x80`/其余。由**上游排版层（bank 61/62，入口 `$A4D7`）经跳转表处理**，渲染前已把句子切成行、建好 `$04E8` 行指针表。故推断为**行/页级**语义：换行、翻页等待、光标定位、角色名、立绘、颜色等（精确含义待追 bank 61/62 跳转表）。宽度上限 `#$1E`=30 tile/行、≤8 行段。

## 中文化改造评估

**有利（确认）**：tile 映射恒等（无需改字节→tile 逻辑）；字库本就可 bank 切换、对话框区独占 256-tile 页；引擎已是"每字一格、可带上格标记"结构；数据侧回写数学已由 msgtool 备齐。

**瓶颈**：任一光栅区一时刻只能寻址 1 个 4KB bank = 256 tile；CHR 是 ROM 不能运行时拼。中文 1500–3000 字远超 256。

**路线（按性价比）**：
1. **MMC5 扩展属性模式（`$5104=1`，ExGrafix）** — 最正统。ExRAM 为每 tile 提供 CHR bank 高位，逐 tile 选 bank，整 512KB CHR 当大字库（《恶魔城传说》日版、《Just Breed》同款）。改动：(a) `$5104` 切扩展属性；(b) **把 ExRAM 工作变量（`$5C00-$5FFF`，含 `$5D1B/1E/21` 文本基址表、`$5DC0-$5EF0` 对象数组等上百处）整体迁到 `$6000-$7FFF` 电池 WRAM**；(c) 每写 tile 号时同步写对应 ExRAM 属性（bank）。难度：中偏高，主要成本是 ExRAM 搬迁。
2. **每行独立字库 bank**（复用现有分屏 IRQ，不动 ExRAM）：对话框区细分光栅、逐行切 `$512B`；需打包期逐行装箱、多插 IRQ、重排分屏表。难度：中。
3. **字号放大（正交）**：8×8 太小；8×16 利用现有上下双格几乎免费（把中文上格也填字模，宽度不变）；16×16 最清晰但要改排版层 `$A4D7`。

**结论**：数据侧可控（msgtool 已铺路）；主成本在 CHR 寻址扩展，走扩展属性模式是最干净通路，ExRAM 搬迁是一次性重构。渲染核心 `$F0B7` 逻辑清晰、恒等映射、改动面小且集中。属"有明确技术路径、需一次中等规模引擎补丁"，非重写文本系统。

## 概念验证 (PoC) 结论 — 2026-07

把句 62（开场艾莉娜台词）patch 成中文 8×8 字模的实验，在 Mesen 里逐帧核对，确认与暴露：

**确认（实证）**
- **tile 恒等映射**被 Tilemap Viewer 实证：屏上 PPU tile index `$B7` 直接对应 CHR 内 tile `$B7`（bank 内偏移 `0xB70`）。
- **每个场景各自加载字库 bank**（不是全程共用 bank 0）：标题幕 = bank 0、海边幕 = bank 11、**开场机甲幕 = bank 48**。同一码位要在用到它的每个场景的字库 bank 里都写一份字模。
- **字库位平面格式**：plane0 全 `0xFF`；plane1 里 **笔画 = 位 0（色号 1，显示为紫）、背景 = 位 1（色号 3，显示为黑）**。造字模若写反（笔画落位 1）→ 颜色反相成"紫底黑字"。正确写法：plane1 先全 `0xFF`，再在笔画像素处 **清零** 对应位。
- 8×8 字模注入正确 bank + 就地改文本 → 模拟器正确显示中文（黑底紫字，与旁边日文一致）。**管线打通。**

**三个坑（决定下一步）**
1. **8×8 太糊**：笔画多的字（觉/意/源）在 8×8 下糊成一团、难辨认。必须上 **16×16** → 需引擎改造（见上文路线 1/3）。
2. **字典字符串重叠**：block `007A`/`0066`/`818F` 等的字符串指向 ROM 里**重叠的同一段字节**（如 `0x056021-22`，极致 MTE 压缩）。就地覆写一个 block 会踩坏共享字节的邻居，导致译文错位（实验中「从/的」错显成「得/觉」）。**结论：回写工具必须整体重建字符串数据区，绝不能就地覆写。**（已由 `reinsert.py` 解决：新数据全部重建到空闲区。）
3. **CG 与字库是不同的 CHR bank（分屏）；最初"机甲变中文"是写错了 bank**（已由实机 + 渲染定论，推翻本坑早期"共享全满"的推断）：
   - **对话框字库 = bank 0**；**机甲 CG = bank 48**；两者靠光栅分屏各用各的 bank。铁证：tile `$83`（片假名 エ）在 **bank 0 是清晰字形、在 bank 48 是 CG 噪声**——对话框显示出 エ，故对话框读 bank 0。
   - ⚠ 工具坑：**Mesen Tilemap Viewer 的 CHR 地址不追 MMC5 bank**（报 `$0830` 误导以为 bank 0）；**Tile Viewer 的地址才含 bank**（`$30830` = bank 48）。判 bank 用 Tile Viewer。
   - 最初 PoC 往 bank 0/11/48 都写、bank 48 是 CG → 机甲变中文。**只往 bank 0 写 → 对话框中文、机甲完好、零副作用**（已实机验证）。

## 里程碑 2：单句正确回写（实机验证）— 2026-07

`reinsert.py` 把句 62 正确回写为中文并在 Mesen 跑通：重建到空闲区（避开字典重叠）+ text 指针反解 + 块串就地重建 + 字模只写 bank 0。**对话框清晰中文、机甲 CG 毫发无损、其他句零副作用**。单场景 demo **无需扩 CHR / 切 bank / 改引擎**。

**但两个上限仍在，全量翻译绕不开引擎改造（B）：**
- **256 tile/bank**：bank 0 已近满，demo 是覆盖了原版低频汉字位（`0xB0–0xC5`="以井宇…"）塞中文——别处日文用到这些字会跟着变（demo 可接受，全量不可）；1500+ 唯一字装不下一个 bank。
- **8×8 太糊**：清晰需 16×16。
两者都要靠"扩 CHR（已验证 1MB 可行）+ 文本段切专属新 bank"解决。

## 里程碑 3：B 命门打通——文本段切到扩 CHR 专属 bank（实机验证）— 2026-07

**对话框是分屏的 region-1**（agent"整屏 bank48"被实机推翻）：region-0（上，机甲）bank = `$0450` = 48；region-1（下，对话框）bank = `$0451`（复位默认 0）。NMI 每帧把 `$0450/$0451` → 影子 `$045E/$045F`，IRQ 按区把 `$045E,Y` → `$512B`。

**打通方法（单处等长改）**：`.nes 0x7EB8F` 的 `AD 51 04`（LDA $0451）→ `A9 80 EA`（LDA #128; NOP），使 region-1 影子 `$045F` = 128。于是对话框读扩 CHR 的 **bank 128**，机甲（region-0 = 48）与 bank0 均不受影响。

**实机验证**（`build_b_demo.py` → `MSG-b-demo.nes`）：中文只放 bank128、bank0 原封不动 → 对话框显示正确中文、机甲 CG 完好。**证明文本段可切到扩 CHR 的专属新 bank。**

→ **至此全部技术难关验证通过**（导出／回写／字库／bank 机制／扩 CHR／文本段专属 bank），剩余为工程量：全量翻译、全量回写、多 bank 字库管理（>256 字）、16×16 渲染（改排版层 `$A4D7`）。

## 16×16 改造方案（agent 静态逆向）— 2026-07

**结论：只改一个例程（字符发射循环 `$F135–$F185`），原地覆写，其余零改动。**

为什么这么省：引擎当前已是"2 tile 高"渲染（8×16 = 上格标记 + 下格字形，本质 2 行 × N 列 blit，`$04A4`=行数恒 2）；VRAM 队列 `$E270` 是**宽度无关的垂直条带 blitter**（dialog 用 `flags=$01` 转置，按 2 字节/条带铺 `width` 列，+32 垂直递增）；宽度 `$04A3` 由 `$A6D2: LDA $14; LSR` 自动推导。所以 16×16 只是"每字吐 4 tile + 每字占 2 列"，`$E270`/参数块/NMI/断行阈值全不动。

**emit 新逻辑**（每字 4 字节 `[TL,BL,TR,BR]`，游标 `$14` +4，列 `$1A` +2；删掉浊音处理腾空间；~62 字节 ≤ 原 80）：
```
读 c ($0469,Y) → base=c<<2 (ASL ASL) → 写 base,base+1,base+2,base+3 到 $04A8,X
测量路径(MEAS)与发射路径都 +2 列/字 → #$1E=30 列阈值不变 = 15 字/行
```
- **字库排布**：每字 4 tile 连排 `4c=TL, 4c+1=BL, 4c+2=TR, 4c+3=BR`；`c<64`（**每 4KB bank 只装 64 字**，密度 256→64）。PPU 码位从"tile 号"变成"字号 c"。
- **代码空间**：emit 原地放下（62≤80，固定 bank 63），无需跳板。
- emit 起点 `.nes 0x7F145`（PRG 0x7F135）。

**风险点（agent 标注，待实机确认）**：
1. `$F0B7` 有第二个 caller `$CC1B`（bank 62，`.nes 0x7CC2B`）也按 2 字节/格填 `$04A8` 并比较 `$0557,X`——改 emit 为 4 字节/字会影响它。需判定它是对话文本（一并 16×16）还是独立 8×16 子系统（需模式标志门控）。**唯一可能扩大工作量的因素。**
2. 打字机逐字显示慢约 2×（VRAM 数据翻倍，观感非 bug）。
3. 行距：行高仍 2 tile，16×16 行可能纵向贴合，观感待核对。

**实测尝试 1（2026-07-05，`build_16x16.py` → `MSG-16x16.nes`，未成）**：
- 现象：对话框单行、半角片假名/汉字碎片混杂、8×8 单高、非中文（那些片假名 = bank128 `tile[0x80+]`，正是**块串的 block 编号字节 `80 81 82…` 被当 tile 直接画**了）。
- 诊断：**数据侧全对**（块串→block→字形码 c=1..27→bank128 `tile[4c]` 中文字模，Python 验证无误）；字模 / B 命门(NMI region1→128) / 回写都对。**问题在 emit 汇编 patch。**
- 根因：**agent 的 emit 62 字节清单与真实 `$F135` 不符**。真实原字节 `.nes 0x7F145`：`a0 01 a5 14 10 1b c6 12 10 03 4c d7 f0 b9 69 04…`（`LDY#1; LDA$14; BPL; DEC$12; BPL; JMP$F0D7; LDA$0469,Y`）——agent 清单假设 `a5 14 30 2d` 开头。覆写不对接、渲染流程被打乱。
- **下一步：重新精确反汇编真实 `$F135` 全段**（不靠 agent 清单），看清 `$0469/$04A8/$12/$14/$1A` 真实用法与 caller 约定，再设计 16×16 emit 改动。数据侧（`build_16x16.py` 的字模/字形码/回写/B命门）已验证可复用。

**尝试 2（2026-07-05，亲手反汇编后整段重写，待实机验证）**：

真实 `$F0B7–$F190`（亲手反汇编 + CDL 佐证，取代 agent 清单）：
- 入口 `$F0B7`（A=输出游标→`$14`，`$27←$FE`）/ `$F0C0`（测量模式 `$14←$FF`）；`JSR $EF96` 解句→`($18)`=块串指针。
- 块循环 `$F0D7`：`($18)` 首字节 `00`=终止(经 `$E226` 收尾，X/Y=总列数)、`≥80`=双字节块(块号=`(b1&7F)<<8|b2`)、其余=单字节块；`JSR $EFB9` 解码到 `$0469`。
- 解码串格式：`$0469[0]`=长度→`$12`，数据 `$046A` 起（Y 从 1）。首字节 `≥80` 走 `$F11E` 特殊路径：`$12=首&7F`，且 `$0469[1+len]` 加到 `$18`——即**文件侧 `00 NN` 前缀串**（结构块 0x21「エリナ 「」等），带块串 gap-skip。
- **`$F135` 只判一次模式**（`LDA $14; BPL`）分成两个独立循环：
  - 测量 `$F13B`：每码位 `INC $1A`（16 位 `$1A/$1B`），`0E/0F` 浊点码不计宽；
  - 发射 `$F156`：每字符写 **2 槽 `[$27(标记,默认FE), 字形码]`** 到 `$04A8,X`，X+=2；码位 `0E/0F` 存入 `$27` 作为下一字符的上标记（**跨 block 持久**——浊点前缀与字形可分属两个字典块）。
- 出口：计数尽 → `STX $14`（存游标）`JMP $F0D7`。宽度 `$04A3=$14>>1` 在 `$A6D2` 推导，与此一致。

新 emit（82 字节整段覆写 `$F135–$F186`，尾部 2×NOP，`.nes 0x7F145`）：
- 结构保持"一次判模式+双循环"；删浊点逻辑（中文不用）。
- 测量：每码位 `INC $1A` ×2（每字 2 列）。
- 发射：`c=($0469,Y); A=c<<2`（ASL×2，c≤63 保证 C=0）→ 连写 `4c,4c+1,4c+2,4c+3` 四槽（`ADC #1` ×3），X+=4，每字 +2 列。四槽=左列上下+右列上下，恰与 blitter 垂直条带序一致。
- 安全性：全 PRG 扫描确认无外部 `JMP/JSR` 进 `$F136–$F186`（3 处疑似均为 CDL 未访问的数据巧合）；patch 后反汇编回读与设计逐字节一致。

**幻影空槽事故（本次新发现，已回修 reinsert.py）**：
- 双字节 text 表槽 `0x3D5C+3*idx` 仅到 `idx=0xB53`；`idx≥0xB54`（文件 `0x5F58` 起）**已是句 1108 的块串数据**（句 1108 指针字节 `02 48 1f`，b1<0x30 的负偏移特例）。
- 即：可引用范围内的双字节块 `0x8080–0x8B53` 全部在用，**真正空槽只有未引用的单字节块 0x20–0x7F 共 25 个**。`0x8B54–0x8B57` 是幻影——里程碑 2 的 `MSG-zh-test.nes` 曾把"这就是次"写进这 4 槽，句 1108 潜伏损坏（40 句抽查未命中；教训：**副作用检查必须全量 2781 句**）。
- 尝试 1 还犯过更粗的错：硬编码 `0x8080+i` 当空槽，踩中引号块 `0x8094/0x8098` 等 → 36 句损坏。

**块结构新知**：块 `0x02` = 空块（文件串 `00 00`，零码位零宽），出现在原句自然断句处 = **折行锚点**（配合 30 列阈值）；句尾控制 `05 82` 保留。块 PPU 串长度上限 31 码位（len 位域 `(b1>>6)<<3|(b3>>5)`）→ **"一段一块"**：3 个段（≤15 字/段）只占 3 个空单字节槽，也正是全量回写该用的粒度。

尝试 2 产物自验：句 62 三层链路 round-trip ✓（30 码位、段界 [12,23]、句尾控制在）；**全 2781 句副作用 = 0** ✓；bank128 = bank0 整拷（边框/空白 tile 保留）+ 27 字 16×16 字模 `tile[4c..4c+3]`。

**尝试 2 实机结果（2026-07-05，Lua 动态调试，未成但定位了根因）**：
- 对话框仍是原版 `[FE, 码位]` 双槽 8×16，非四连号。数据侧再次全对（`$0469 = 0c 01 02 03 04 05 06 07 05 08 09 0a 0b` = 我们的段 1 码位）。
- **Lua 铁证**：emit 那一刻 `CPU $F135` == `PRG 0x7F135` == 我的 patch（`a0 01 a5 14 10 15…`），即我的码确实映射并执行。**但填 `$04A8` 的不是它。**
- **真凶：对话正文由 bank 62 一套独立的打字机 emit 渲染**（`$D2ED`–`$D346`），不经过 `$F135`。全 ROM 搜 `B9 69 04`(LDA $0469,Y)：bank 63 三处（我改的 `$F135` 一族）+ **bank 62 四处已执行**（`$D20F/$D2F1/$D98C/$D9EA`）。
- **`$F135`/`$F0B7` 的真实用途 = 名字/菜单排版**（bank 61 `$A6CF` 调用），不是对话正文——之前"改一个例程即可"的判断基于 agent 的错误定位。
- **bank 62 对话 emit 结构**（`$D2F1` 起）：读 `$0469[Y]`→ `0E/0F` 浊点特判（存 `$27` + `$9348` 设浊点 tile）→ 否则 `STA $04A9,X`(下格=字形) / `LDA $27; STA $04A8,X`(上格=标记,默认 FE) / `LDA #FE STA $27`；关键是它与**逐字显现状态机**深度耦合：`$2D`=列游标、`$37`=本次显现字数、`$0557,X`(索引 `$14+table[$33]`)=显现缓冲影子（`$CC2B` 拿它和 `$04A8,X` 逐列比较驱动打字机）。宽度 `$04A3`、行数 `$04A4=2`、列计数全部按**1 列/字**设计。
- **含义**：真 16×16（每字 2 列 4 tile）要改的不是一条例程，而是**整个 bank62 打字机子系统**（列宽、显现数组、字数计数、`$D2ED` 发射、`0E/0F`），且有第二/三调用点。这是一个有实际风险的子工程，非"原地覆写 62 字节"。
- 相较之下 **8×8-塞进-8×16-下格 的中文已在里程碑 3 实机验证可读**（`build_b_demo`，用户"是中文！"）；那条数据侧管线零汇编风险。`build_16x16.py` 的字模/B命门/回写全部可复用，只是发射目标要换成 bank62。
- 下一步已交用户决策：(A) 啃 bank62 打字机做真 16×16；(B) 数据侧管线先交付 8×16 可玩版，16×16 作为后续 polish。

## 里程碑 4：8×16 全高中文实机成功（最小改法）— 2026-07-05

用户选「先试最小改法」。**保持 1 列/字**（显现数组/列宽/字数计数全不动），只把 bank62
两条对话 emit 的「上格取 $27(FE)」改成「上格 = 字形码 c|0x40」——此时 A 恰为刚写过
$04A9 的 c，一条 `ORA #$40`(09 40) 覆盖 `LDA $27`(A5 27) 即可。于是每列 = 上半 tile
`0x40+c` + 下半 tile `c` = **8 宽 × 16 高全高汉字**。

- 两处 patch（file 偏移，各 2 字节 `A5 27`→`09 40`）：
  - `0x7D336`（CPU $D326，缓冲打字机 emit）
  - `0x7DA0C`（CPU $D9FC，逐字即时 blit emit，结构对称的第二条路径）
- **不碰 $F135**（那是名字/菜单排版，与对话正文无关；尝试1/2 白改了它）。
- 字模：`glyph8x16` 把字渲染成 8×16，上半→tile `0x40+c`、下半→tile `c`（c≤0x3F，故 128 字/bank）。
- 数据侧沿用里程碑2/3：一段一块、单/多码位串、text 反解、B命门 NMI patch、句62 三段重写。
- 自验：句62 round-trip ✓、段界 [12,23] ✓、**全 2781 句零副作用** ✓。见 `build_8x16.py`、产物 `MSG-8x16.nes`。
- **实机（用户）：「是中文！虽然字体怪怪的」** —— 渲染管线全对，机甲完好，三行断句正确。
- 「怪」的根因 = **8 像素宽是硬物理限制**（横向笔画挤压）+ 字模字体/阈值未调优，与引擎无关。
- 清晰度天花板：8×16 到顶就这样；要更清晰需 **双宽真 16×16**（啃 bank62 打字机子系统：列宽×2、
  显现数组按 2 列/字、字数计数、$D2ED 四连号发射、0E/0F 与第二调用点 $D9EA）。

### 字模：改用像素字体（缝合怪 Fusion Pixel Font）— 2026-07-05

矢量字（黑体等）硬缩到 8px 会糊成一团（"字体怪怪的"根因之一）。改用**手工 hint 的像素字体**
[Fusion Pixel Font](https://github.com/TakWolf/fusion-pixel-font)（缝合怪，OFL 授权，随仓库分发于 `fonts/`）：
- **fusion-8px** 原生 8×8 CJK，无抗锯齿直接提点阵，8 宽也清晰可读 → 现 `build_8x16.py` 用它，
  8×8 竖直居中（cell 行 4..11）拆进上下 tile，实机对比 vs 矢量糊团是天壤之别（见 `preview/`）。
- **fusion-12px / ark-12px** 12×12 complete；将来双宽 16×16 用 fusion-12px 居中进 16×16。
- **ark-16px 不可用**：zh_cn 缺大量 CJK（渲染全 .notdef 豆腐块，每字 60 px = 16×16 边框周长）；
  Fusion 存在的意义正是缝合补全 ark 的缺口，而 16px 档只有 ark、还不全。
- 供字选择：`fonts/fusion-pixel-8px-monospaced-zh_hans.otf`（2.3MB）、`-12px-`（4.9MB）+ `OFL.txt`/`LICENSES/`。

### 引擎定案：原版 8×8 机制（零 emit 改动）— 2026-07-05

用户实机确认 fusion-8px「非常清楚」，定 8×8。引擎在两条路里选**原版 8×8**（而非 8×16 上下留白）：
- **撤掉里程碑4 的两处 emit patch**（`$D326`/`$D9FC` 还原 `A5 27`）——对话 emit 完全保持原样：
  上格 = `$27` = FE = 空白黑 tile（实测 bank0 tile FE 的 plane1 全 FF = 全背景色），下格 = 字形 tile `c`。
- **每字仅 1 个 tile**（下格 c），字库密度 ~238 字/4KB-bank（8×16 留白方案要 2 tile/字、仅 63/bank，全量费 4 倍 bank + 多 bank 管理复杂）。
- 字竖直贴下排（cell 行 8..15），与原版假名位置一致。
- **整份 ROM 的汇编改动收敛到唯一 1 处 = B 命门 NMI patch**（`0x7EB8F`），其余全是数据回写。极简、极稳。
- 字形码 c 从 `0x10` 起，避开 PPU 特殊码 `0x00/0x0E/0x0F` 与空白 `0xFE`；全量多 bank 装箱须按 bank 避让边框 tile（实测 `0xD1/0xD9` 等高位）。
- 见 `build_8x8.py`（原 `build_8x16.py` 改名）、产物 `MSG-8x8.nes`。双宽 16×16 方案（`build_16x16.py` + bank62 打字机重写）留作以后 polish。

## 里程碑 7：忠实结构化管线（演出全还原）— 2026-07-05

**根因**：原 `sentence_text` 拍平导出丢了折行(0x02)与设置块，导致演出（换行/分页/区域初始化）无法还原。
**演出全由结构块驱动**（里程碑逆向结论）：名字块=颜色/缩进/说话人、折行 0x02=换行+分页、
前导设置块(0x01/03/04/06/11/15/19/52)=区域/对话框初始化、`~XXXX~`=停顿/等待/颜色等位置相关效果。

**方案（三件套，已在第一章试点实机验证）**：
1. `structio.py` **结构化导出**：折行→`/`、设置块→`{sHH}`、控制码→`~XXXX~`、名字/引号/文字保留。
   round-trip 2781/2781；控制码 896 种全覆盖、无未知漏网（去掉 `/`+`{sHH}` 后逐字等于 `sentence_text`）。
2. **结构化翻译**：agent 在结构化形式上把折行 `/` 放到中文短语边界（参照日文位置）、控制码/设置块原样保留。
   —— 折行等"位置相关"结构无法全自动，靠翻译时人/agent 判断，这是忠实的关键。
3. `build_ch1.py` **忠实回写**：读结构化中文，`/`→0x02、`{sHH}`→设置块、`~XXXX~`→字节、名字块中文化
   (保 [00,X] 长度→颜色/缩进不变)、引号 tile 0x11/0x12 保留、块级结构原样。**不再靠启发式猜折行**。

**实机结果（第一章开场弧 55-110）**：折行落在原版位置、颜色/缩进/打字机速度+音效全对，
**且右下角"等待翻页 ▽"恢复**（上一版启发式丢了控制/设置块 → 自动跳场景；本版保留 → 按原版等输入）
= 结构保留的忠实性得证。句 109 超单 bank 容量暂留日文（全量多 bank 解决）。

**剩余**：① 顶部绿字等**多区域字库**——数据(nametable)是对的中文，只是该区域激活的 CHR bank≠128
（Tilemap Viewer 在扫描线 240=bank128 显示正确，屏幕顶部走 CG bank → 乱码）；修法=给该区域路由到字库 bank。
② 推广全量：结构化导出全剧本 + 逐章补折行 + 多 bank 装箱 + 块串空间（可达区限制，需共享块或就地）。

### 全量 B 的技术准备（agent 静态定位，确认级）
- **背景字库 bank 的硬件写入咽喉**：NMI `.nes 0x7EB6B` `STA $512B`（帧起始默认，取自 `$0450`）；IRQ `.nes 0x7FF47` `STA $512B`（分区，取自 `$045E,Y`）。
- **bank 号来源**：场景画面数据流命令 `70 <bank>` → 处理程序 `$8307`（PRG `0x76307`）写 `$0450`；标题 `70 00`/海边 `70 0B`/机甲 `70 30`。
- **改法（方案 A，待实机验证）**：在文本段扫描线分屏处追加 `LDA #<新bank>; STA $512B/$5123`，把文本段切到专属中文字库 bank；帧顶 NMI 用 `$0450` 复位，CG 段不受影响。
- ⚠ agent 曾判"机甲幕整屏单一 bank 48、无 CHR 分屏"——**被实机推翻**（对话框实为 bank 0），说明对话框区存在 agent 静态未捕获的 bank 切换；全量改造前需在此处补动态确认。

PoC 脚本：`patch_poc.py`（实验性，依赖本地 ROM，产物 `MSG-zh-test.nes` 已 gitignore）。

## 里程碑 5：全量回写工具 Phase 1（通用编码器）— 2026-07-05

`reinsert_full.py`：消费译文串（中文 + `~XXXX~` 控制码 + `(爱心)★` 图标），重建三层指针链回写整份剧本。
- **架构定案：每字形一个共享 block**（学里程碑 2，非"每段一块"——后者 block 数=段数会爆表）。
  唯一字形 → 双字节 block（PPU 串=单码位 [tile 码]），全篇复用；块串 = 控制码字节 + block 序列。
- **控制码 `~XXXX~` 原样还原字节**；图标 token 复用 bank128 原 tile（`(特1)=0x15…★=0x1A`，fusion 画不出）；其余每字 fusion-8px 新 tile。
- **空间布局**（四结构避撞）：句子表 `0x1CC5`；文本表 `0x3D5C`（≤`0x5F58`）；PPU 串区 `0x5F60–0x76000`（`string_pointer` 可达 ~`0x4A010–0x76010`）；块串区 `0x76000–0x100010`（扩 PRG→1MB，`block_string_pointer` 可达 `0x10E00F`）。
- **full-reclaim 模式**：全量替换整个剧本 → 原字典 block/文本区/块串区全部回收。
- **实测（原文当译文自验）**：编码 409 句、233 唯一字模、块串 24KB，**round-trip 409/409 全对 ✓**。
  409 句处 `StopIteration`——**唯一瓶颈 = 单 bank 字模码池 ~236 个**（撞一个 CHR bank 上限）。
  即：编码器 + 空间布局全解决，block/串空间绰绰有余，**剩下只差多 bank 字库**。

### 里程碑 6：全量回写多 bank 数据侧完成（round-trip 全对）— 2026-07-05

全 2781 句译文（1472 唯一字）回写打通，`reinsert_full.py` 重写为**装箱架构**：
- **每句字形必须同 bank**（对话框一屏只显示一个 CHR bank）。first-fit-decreasing 把句子按词汇相似度装箱：
  码池 216（`0x10-0xCF`+`0xE0-0xFD` 去图标；预留 `0xD0-0xDF` 边框/`0xFE` 空白）→ **106 个 bank（CHR 128-233 < 可用 128）** ✓。
- 每 bank = bank0 拷贝（保边框/空白/图标）+ 该 bank 字形；PPU 码 → 共享 block（渲染按激活 bank 取 tile）。
- **实测：回写 2776 句（除 4 空句 + 1 超长句 380），round-trip 2776/2776 全对**；产物 `MSG-zh-full.nes`（2MB）。
- 句 380（750 字、217 唯一字 > 一个 bank）需按页拆，单列待办。

**仅剩最后一步 = 每句设 CHR 字库 bank 的 asm 钩子**（`sent_bank[n]`）：
- 存「句号→bank」表（2781 字节）入 ROM；在句子显示路径查表 → 写一个「当前字库 bank」变量；
  B 命门 NMI 改成读该变量（而非硬编码 128）。
- 待 RE：句号在显示时的可取处（sentence-display 触发点）。当前 ROM 所有句读默认 bank 128，
  故仅分到 bank 0 的句子显示正常，其余错乱——这是全量可玩版的最后一关。

### 里程碑 6 勘误：装箱粒度 = 「同屏」而非「每句」 — 2026-07-06

里程碑 6 写的「每句字形必须同 bank」是**错误近似**（用户指出：屏幕能同时显示多句）。真相：
- NES 每个背景区一帧只能用 1 个 CHR bank(256 tile 页)。本作靠**光栅分屏**让不同区域用不同
  bank（开场屏 = 绿字 bank0 + CG bank103 + 对话框 bank128，三段各一 bank）。
- **对话框区是一整段扫描线、只有一个 bank**。故约束是「**同一时刻对话框内显示的所有字（哪怕跨
  几句）必须同 bank**」，不是每句。「每句一 bank」既过保守（一屏多句时），又会出错（同屏的
  句 64/65 若分到不同 bank，一半字变乱码）。
- 原版从不切对话 bank：日文用两级字典压缩，256 tile 够复用；中文 1472 唯一字才产生多 bank 需求。
- **正确装箱单位 = 一屏对话框内容（翻页单位）**，切 bank 只能在「清屏翻页」边界。
- **好消息（实测）**：一屏仅 ~56 全角字；第一章按剧情场景分段，每段唯一字 62~181 全都 < 一个
  bank(213)。故可**按场景装箱**，切 bank 点用游戏现成的场景切换（背景 CG bank `$0450` 变化，
  NMI 每帧照它载 CHR bank）——只需在 NMI 加个 `$0450→中文字库 bank` 小查表（或块串内切 bank 码），
  远比「句号→2781 字节表 + 句读取钩子」简单。第一章 320 字 → 2~几个 bank。

**（历史）Phase 2 早期设想**：全剧本估 ~1200–1800 唯一汉字 → 需 6–10 个 CHR bank。
两条路（择一，需先有真实译文词表做装箱）：
- **A 每句一 bank**：按词表把句子聚类，使每句用到的字形都在同一 bank；每句渲染前设置字库 bank。
  改 B 命门让它读一个「当前字库 bank」变量（而非硬编码 128），并在每句解析处按「句→bank」表写该变量。
  需找每句钩子（`$EF96` 一带是块串指针解析 + `STA $5115` PRG 分页，非干净按句号，待细化）。
- **B 串内切 bank**：新增/复用一个块串控制码，其处理程序写 `$512B`（BG CHR bank），块串按 bank 分段插切换码。
  更通用（一句可跨 bank），但需 RE 控制码派发 + 与光栅分屏/NMI 的 bank 协作。
- 备选 C：MMC5 ExRAM 逐 tile bank（属性扩展模式）——理论可近乎无限字，但要启用大特性，风险最高。

### 里程碑 8：绿字之谜破解 + 无头模拟器取证闭环 — 2026-07-05

开场顶部绿字（句 63「寻找创造主 地球正濒临危机」）只显示 5/12 字，排查三层后全修：

1. **绿字区 CHR bank**：该幕 `$512B` 每帧光栅三段——CG=bank103、对话=bank128（B 命门补丁）、
   **绿字=bank0**（vblank 尾写 0，覆盖 scanline 0-24）。→ 中文字库拷进 bank 0 即可；
   **bank 103 是该幕 CG 美术，千万别动**（全部 128 个原 bank 画成图集看过：成套字库只有
   bank0=对话字库、bank10=标题字体，其余全是 CG——`reversing/chr_atlas.py`）。
2. **假线索排除**：nametable 码全对（绿字写在 NT $2000 **行 25**，靠 scroll 卷到屏顶）；
   字库 tile 全在位；缺字与 tile 码区间无关——但与 **16×16 属性对完美相关**（偶对隐形/奇对显示）。
3. **根因 = 空隙块结构语义**：绿字行属性原版全 `$FF`（调色板3=绿），我们的构建是 `$FE`
   （TL 象限=调色板2=全黑 → 偶数 16px 列隐形）。差异来源：句 63 行首的**空隙块 `0x4B`
   （PPU=[00,03,FE,FE,FE]）**——引擎读它写属性 `$FF`。同内容异 ID 的空隙块有一堆
   （3D/48/4B/4C/59 全是 [00,03,FE³]），**块 ID 本身携带演出语义**，与名字块同理。
   而 build 曾把 0x4B 当空闲块挪用给字模 → header 消失 → 属性 fallback `$FE`。
   **修复**：一切结构块（PPU 串以 00 开头：名字/空隙/设置）保留不进分配池；
   行首空格串回写为原句的空隙块 ID（`orig_leading_gap`）。

**无头取证闭环（新方法论，勿再手动来回截图）**：`mesen_headless_green.lua` +
`Mesen --testrunner <rom> <lua>`。要点：`print()` 直通 stdout（`emu.log` 在 testrunner 不可见）、
Lua 沙箱禁 `io`（输出/截图全走 print，截图 hex 编码）、`emu.setEmulationSpeed(0)` 不限速
（整轮 7 秒）、开场动画在 NEW GAME **之前**自动播放（什么都不按等它来）、
检测 NT 连续码序列防 CG 撞码假阳性、**setInput 必须在 inputPolled 回调里**（startFrame 里无效）、
**exec 回调必须传 start,end 两个地址**（`addMemoryCallback(fn,exec,a,a)`）。
dump：MMC5 全寄存器+ExRAM+双 NT 30 行+属性表+调色板+截图。

### 里程碑 9：句号 trace 打通（移植自 2021 FCEUX 脚本）— 2026-07-06

用户 2021 年的 `自动化.lua`（FCEUX，未写完/跑不到通关）里藏着句号读取钩子，移植到 Mesen
成立（`mesen_sentence_trace.lua`）：
- **钩子点 $F071**（exec）：游戏读句子指针时经过；zero-page **$87/$88** = 当前句子指针。
- **反算**：`$87 ∈ [$BCB5,$BFFF) 且 $A000 首字节==PRG[0]($00,bank0)` → 句号 `($87-$BCB5)/3`（主表 1~281 区）；
  `$87 ∈ [$A000,$BD4B) 且 $A000 首字节==PRG[$2000]($36,bank1)` → 句号 `($87-$A000)/3+281`（追加表）。
  （bank0 首字节 $00、bank1 首字节 $36 是 rom.readbyte 判据，实测吻合。）
- **$0450 = 当前背景 CG bank**（非字库 bank）；据此可按场景（`sentence_for_back`）归类句集。
- **实测测绘**：开场=句 61(bank30)→62-65(bank67，含绿字)→回标题；**NEW GAME 正片自句 90 起**
  (bank10 店门口→bank0B 海边)。**推翻"第一章=句55-110"的猜测**——55-89 多为其它场景/菜单。
  完整第一章句集待更聪明的导航（随机按键会卡在海边菜单循环 94/97/102/105/106）。

### 里程碑 10：多 bank asm 钩子攻克——「最后一关」拿下（实机验证）— 2026-07-06

全量可玩版的唯一硬骨头（每句/每场景设 CHR 字库 bank）解决,而且是**单点等长改一条指令**:

- **注入点 = NMI `$EB7F`**(file 0x7EB8F)。反汇编发现进入此处时寄存器 **A 已 = `$0450`**(背景 CG bank),
  因为紧邻的 `$EB79 LDA $0450; $EB7C STA $045E`(STA 不改 A)。故把
  `LDA $0451; STA $045F`(`AD5104 8D5F04`) 改成 **`ORA #$80; STA $045F; NOP`**(`0980 8D5F04 EA`)——
  对话框(region-1)字库 bank `$045F` = **`$0450 | 0x80`**。
- **效果**:每个背景 CG 场景(`$0450`)自动映射到扩 CHR 的 `(cg|0x80)` bank。切 bank 白嫖游戏
  现成的场景切换,**无需查表、无需新代码、无需 bank63 空间**(bank63 实测零空隙,此法彻底绕开)。
- **装箱粒度 = 场景**(用户指出对话框滚动、多句同屏 → 同场景句必须同 bank;实测每场景 < 213 字)。
  每场景字装进 `(cg|0x80)` bank(= bank0 拷贝 + 该场景字模);名字字用固定码位(名字块跨场景共享 block)。
- **实机验证**(`build_ch1_mb.py` + `mesen_verify_mb.lua`,无头 testrunner):
  店门口(CG$10→bank$90)对话框「咦——？ 关着门呢？」忠「刚才还在的 跑哪儿去了呢」**完整正确**;
  海边(CG$0B→bank$8B)艾莉娜/忠对话中文正常。两个不同场景用两个字库 bank、都对 → **切换成功**。
- **全量推广**:同一 CG bank 若被多章节复用会共享字库 bank(第一章内无复用);全量时若某 CG bank
  跨章节字数 > 213,需再细分(可在 `$045F` 上叠加 `$5130` CHR 高位,或按章节偏移)。

采样注意:startFrame(帧初)读 `$045F` 会滞后一帧(NMI 在帧末按当时 `$0450` 设值),稳定场景内
`$045F==$0450|0x80` 成立,切换帧看似 MISMATCH 是采样时序,非 bug。

## 翻译

`translation/GLOSSARY.md`（人名/专名/控制码保留规则，随仓库）。日文原剧本与译文 WIP 按版权 gitignore
（`scratch_script.json`、`translation/batches|out/`、`translation/struct_full.tsv`）。

### 里程碑 11：全量结构化译文集齐（2781/2781，token 0 不一致）— 2026-07-06

在 flat 中文草稿(2781 句)基础上做**结构化重译**——按日文原文结构(`structio.export_structured`)把折行
`/`、设置块 `{sHH}`、空隙块 `{gHH}`、控制码 `~XXXX~`、名字块、引号补回中文。分 12 批(1-54 + 157-2781
按 250/批 + 第一章 55-156)由并行子代理做,每批产出后用 `structio` 的 `_structural_sig`/`_sig_from_structured`
**独立逐句比对日文结构验证**(不只信 agent 自查):
- **2781/2781 句全部结构化**,合并进 `translation/struct_full.tsv`(gitignore)。
- **token 一致性 0 处不一致**(每句 `~XXXX~`/`{sHH}`/`{gHH}` 的数量/顺序/十六进制 + `/` 折行数量
  均与日文严格相等)。
- 发现:flat 草稿其实保留了多数 `~XXXX~` 控制码,普遍丢失的是 `{sHH}` 设置块和全部 `/` 折行——
  结构化的核心工作即按日文位置把这两类补回,吸附到中文短语边界。
- 待打磨(非硬约束):部分长句因中日语序差异,`/` 落点是短语近似(数量精确);省略号 `……`/`......`
  各批不统一,回写时 build 里 normalize。
- **通往全量可玩仅剩「场景映射」**(每句归属哪个 `$0450` 用于装箱,里程碑 10 的 asm 运行时自动切 bank
  已就绪)——最实际靠 GUI trace 玩一遍(testrunner 30000 帧限只在无头,GUI 无),或续攻 savestate 分段+修
  `stage_1` 海边 op10 序列。

（历史）早期 flat 翻译:2781 句切 14 批(200/批),由并行子代理译。

## 里程碑12：说话人头像立绘乱码 —— 根因=安全区选错，已修复（2026-07-07）

**最终结论(已实机验证修复)**：不是演出耦合，是 `build_ch1_mb.py` 的 `SAFE=(0x73604, 0x76000)` **选错了空闲区**——它覆盖了两处存活数据：
- **0x74000-0x75B00 = bank58 的立绘/头像指针表+数据**（$A000 表，`LDA $A000,X`→立绘指针→头像 bank）。句子写这里→梓登场读表得垃圾指针($0006)→头像 bank=00→立绘乱。
- **0x76000-0x80010 = bank59 等存活代码**（启动进 $8000=bank59；覆盖→PC 卡 800F 不启动）。

**修复**：仿 `reinsert_full`，**扩 PRG→1MB**（`rom[4]=64`），句子 block 流写**真·新增区 `BLK_FREE=(0x80010, 0x100010)`**；block 的 1 字节 text 串写真空闲 `TEXT_FREE=(0x73604, 0x74000)`(2556B 够，量仅 202B)；CHR 同扩到 1MB(挪到 0x100010)。**关键坑**：扩 PRG 后上电 MMC5 `$5117=$FF`→末 bank(bank127)映射 $E000-$FFFF，新区是空的→复位向量读 0 崩；故把原 PRG 末 8KB(向量+固定代码)拷到新 PRG 末 8KB。build 加断言 `rom[0x74000:0x75B00]==原版` 防再次覆盖立绘。实机验证：开场对话/梓登场头像立绘(金发脸)/店门口 全部正常中文。

**教训**：选"空闲区"必须核对原版该区是否真空(全 00/FF)——0x73604-0x74000 才是唯一真空闲(2556B)，0x74000+ 全是存活的立绘/代码数据。之前主线对话"正常"是因为不触发立绘系统，掩盖了覆盖。

---
（原诊断记录，已被上面修复取代）
## 里程碑12旧：说话人头像立绘乱码根因（深度诊断）

用户报「点完梓也要去后变乱」的那一屏（句91后半）。**重大认知纠正：文字本身正确，乱的是右下角说话人头像立绘**（原版是梓的金发彩色脸，汉化变黑块+碎片）。8px 字体下「哥→研、要→圣、—→一」是视觉误读，`$8B` bank 静态验证 + `expect_91.png` 渲染均确认字模正确。

**根因链（Mesen getState + 内存 hook 逐层追）**：
- 头像立绘=sprite，region1 sprite CHR bank `$5127 ← $0462 ← $0454`。NMI（file 0x7EB6B/复制循环）把 `$0453/$0454→$0461/$0462`，IRQ（0x7FCDC）`$0461,Y→$5127`。
- 梓登场时 **`$0454` 原版=03（头像 bank），汉化=00（bank0=对话字库）** → 头像 sprite 取中文字库 → 乱。且 OAM 从原版 20 个整齐 sprite 变汉化 42 个散乱（sprite 生成器按错 bank 的非空 tile 建 OAM）。
- `$0454` 由 file 0x7B42E `LDA ($1A),Y; STA $05FD; STA $0454` 设置。指针 `$1A/1B` 原版=`$AA15`（指向递增 bank 表 `01 02 03...` → 03），汉化=`$9796`（指向**句91的中文 block 流** → 读到 00）。
- 指针链前段两版一致（都过 `$9704`，bank60 固定引擎数据，内容相同），**在读取句91数据处分歧**：演出脚本通过被我改过的句子指针表（`$1CC2+91*3`）读句91，跟进到安全区中文 block 流，取到的字节与原版日文流不同 → 指针错 → 头像 bank 错。

**本质**：演出脚本的头像指针**耦合句子 block 流的字节布局/block 计数**（原版日文单字节 block vs 中文双字节 0x80xx block，布局完全不同）。第一章 60 句含演出控制码（block 0x10），14 句含 0xC1 双字节演出块。我的「relocate 到安全区 + 中文重写」必然改变 block 布局 → 凡触发这类头像/入场演出的时刻都会乱。**日常对话（无头像，占绝大多数）完全正常**。

**无头修复受限**：testrunner 只能 vblank 读取（渲染态 CHR bank 不可靠）；`getState()["cpu.pc"]` 在 memory-write 回调里返回值不准（指到数据表区）；演出脚本代码位置未定位。可靠续攻需 GUI+debugger（看渲染扫描线、下代码断点）。

**候选修复方向**（均未验证）：① 对含头像演出的句保留原版日文 block（那几句不中文化，演出正常）；② 深挖演出脚本，patch 指针计算使其不依赖 block 布局；③ 改重写策略为「原地保留 block 结构、只换字模码」（但中文 block 数≠日文，做不到等长）。

## 里程碑13：安全区/UI-tile 一系列修复（2026-07-07，用户实测驱动）

用户玩汉化 demo 连续报一批乱码，逐个查明并修（`build_ch1_mb.py`）：

- **头像立绘乱码根因=安全区选错**：`SAFE=(0x73604,0x76000)` 覆盖了 **bank58 立绘/头像指针表(0x74000-0x75B00)** + **bank59 启动代码(0x76000+)**。改为扩 PRG→1MB，句子写真·新增区 `BLK_FREE=(0x80010,0x100010)`、text 串写 `0x73604-0x74000`，CHR 同扩到 1MB(挪 0x100010)。★扩 PRG 后上电 `$5117=$FF`→末 bank127 是空区、复位向量读 0 崩，故把原 PRG 末 8KB(向量)拷到新 PRG 末。加断言 `rom[0x74000:0x75B00]==原版` 防再覆盖。
- **跨场景乱码**：句显示场景≠trace 场景→用错 bank 的码。用户 GUI 跑原版全章「每帧采样(lastN+$0450)」得每句场景集(`mesen_gui_scenetrace.lua`，持续增量输出 M 行)。据此把海边区 `{0B,10,11}` 分组共享；开场/查米店/短转场跨组句 (65,90-92,94,97,110,113,114,115,122,130,140,141) 加 cross(全局固定码)。命令菜单句其实是共享句号、跨所有场景。
- **UI-tile 被中文覆盖**：`CODE_POOL` 原从 0x10 起，盖了原版 **0x10-0x1F 图标/方向键/♥♫★/边框区** 和 **0x20-0x29 数字区(菜单序号 1-9)**。改 `CODE_POOL` 从 **0x2A** 起，图标+数字保留原版。方向键、菜单序号"3"修好。
- **选项深入对话留日文**：句 183/187(梓聊大海/父母的事)是选项深入对话，自动序列+GUI 采样都没选到→未测绘→skip 留日文乱码。手动补 `gui_scene`=0B。
- **keep 改贪心装箱**：原「全或无」(组超预算则回访全丢)改成主线必留、回访逐句按剩余空间填→排除更多 UI 码位后回访仅少量降级(41 句，穿梭机 210-216 全保)而非整片丢。
- **名字对齐**：引擎按固定起始列渲染名字块(实测右对齐反使短名更靠左)，故名字块内 `lead=0` 左对齐，各名字左端对齐。「列因名字长短参差(引擎左对齐渲染的必然、原版同)，做不到「对齐。

**当前 118 句、回访 41、立绘/启动/向量完好**，文字/剧情/头像脸/名字左端全正常。

**★未解(headless 观测受限)**：①说话人**小头像装饰边框**显示中文("记"/"12")——它是对话区(region1, background=$045F=$8B 中文 bank)的显示，框图案 tile 码被中文覆盖；但框走 sprite 还是 ExRAM nametable、用哪些 tile 码、哪条扫描线切哪 bank，无头模拟器读不到渲染扫描线/运行时精确 bank 映射(NT dump 定位不到、OAM 只见脸 sprite、ExRAM 是 mode1 扩展属性非 tile 码)，卡在观测。②名字「列不对齐(同前，引擎左对齐必然)。两者均纯装饰、不影响脸/文字/剧情。彻底修需 GUI+debugger(PPU/Tilemap Viewer 看框实际 tile+bank) 或 asm 分屏精细化(框行用原 bank、文字行用中文 bank，风险高)。

## 工具

`reversing/` 下：`dis6502.py`（6502 反汇编器 + ROM/CDL 加载）、`scan.py`（PPU/MMC5 寄存器写入扫描）、`chrview.py`（CHR tile 渲染验证）。
