# 脚本引擎逆向(白盒反编译进行中)

目标:直接从脚本 bytecode **静态推导完整遍历图**(所有场景/对话/分支),取代动态黑盒遍历。
判定实验证明游戏是"脚本流"(状态路径相关,无法状态签名去重),故动态遍历只能穷尽采样;
而 bytecode 是确定的,静态反编译能给出**可证完整**的遍历图。本文记录已确认的引擎结构。

## ★两套独立系统(关键认知)

第一章的流程由**两套互不相同的系统**驱动,遍历图分两半:

| 系统 | 管什么 | 遍历图的哪一半 |
|---|---|---|
| **过场脚本解释器**（`$8012` 主循环）| 场景切换、过场动画、CG、区域设置 | **场景推进 / 「前往」**（切场景命令）|
| **对话菜单系统**（`$87/$88` 句子指针 + `$2C` 光标 + 菜单派发）| 场景内话题菜单、对话显示 | **场景内话题分支** |

实测:海边菜单交互全程 `$8012` 命中 **0 次** → 对话菜单**不经过**脚本解释器。两套要分别逆向。

## 一、过场脚本解释器(bytecode,已基本摸清)

- **脚本指针** `$C6/$C7`(CPU 地址,走 `$A000-$BFFF` 窗口)、**脚本 bank** `$C5`（写 `$5115`）。
- **主循环** `$8012`：`LDA ($C6),Y` 读命令字节 → 高 4 位查类别处理表 `$8079` → `$8099[高4位]`
  决定"低4位是子命令(=0)还是参数(≠0)" → 取 3 字节项(长度+处理程序地址)→ 读参数 →
  `JMP ($00B9)` 跳处理程序 → 处理完 `JMP $8012`,指针已 `+= 命令长度` 前进到下一条。
- **命令表**：类别处理表基址 `$8079`(16×2B)、子偏移 `$80A9`(`00 03 06…2D`,每项 3B)。
- **已确认命令**：

| 命令 | 作用 |
|---|---|
| `$01 <ofs16>` | **调用 CALL**：存返回 `$C6/C7→$C8/C9`,再 `$C6 += ofs`（相对）|
| `$02 <ofs16>` | **跳转 JMP**：`$C6 += ofs`（相对偏移,非绝对地址）|
| `$60-$6F` / `$70` / `$80` / `$B2` | **切场景**：`LDA $BB; STA $0450`（`$BB`=参数=目标场景 bank）= **「前往」的底层** |
| `$B4` / `$B5` | 设句子指针 `$87/$88`（触发对话显示）|

- **切场景处理程序** `$8307`：`LDA $BB; STA $0450; JMP $8012`。这就是黑盒试探认不出、代码里
  一清二楚的"前往"——一条脚本命令把目标场景号写进 `$0450`。

## 二、对话菜单系统(核心在 bank61,已挖到数据层)

菜单光标 `$2C` 的读写全在 **bank61** → 对话菜单系统在 bank61。

- **菜单主循环** `$885A`：`JSR $E057` 读手柄 → `$062F` 按键 `AND #$CF` 分派:
  `$80`(A)→`$88A0` 选中 / `$40`(B)→`$88F5` 返回 / `$08/$04/$02/$01`(上下左右)→`$890C`+ 导航。
- **A 键选中派发** `$88A0`：`LDX $21`(层深)→ `$2C`(光标)`×2 + $0529,X`(本层选项表基址)
  → `LDA $04E8,Y` 取选项的目标指针 → `INC $21`(层深+1,深入)。
- **菜单栈**(层级结构):`$21`=当前层深;每层一套 `$0529/$052E/$0533,X`(选项表基址/指针)、
  `$0542,X`(该层选中项);**`$04E8` = 选项指针表**(每项 2 字节,指向选项定义块)。
- **选项表填充** `$83EA`:从 `$1E/$1F`(指向菜单定义)读选项块链,每块头 2 字节=步进长度
  (`$64/$65`),块地址存入 `$04E8`,`$1E += 长度` 到下一块。

### ★菜单定义数据格式(遍历图的源头,动态解码得)

菜单定义在 **`$A000-$BFFF` 窗口**(`$5115` 映射,菜单态实测 bank=143,≠脚本 bank `$C5`;
**只能动态读或先读 `$5115` 再定位,勿按 `$C5` 静态算**)。实测海边(`$04E8`=`$BF5B/$BFA5/$BFEF`):

- **每个选项块** = `标签句号(2字节小端,Lua系)` + `内容 bytecode`。
  标签验证:`$BF5B`→句94=查看、`$BFA5`→句97=交谈、`$BFEF`→句102=回头看(Lua 94/97/102 = tr 95/98/103)。
- **内容 bytecode** = `前缀字节 + 标签句号` 序列:
  - **`$50 <句号>` = 一个话题子选项**:查看块含 `50[105大海] 50[106山丘那边] 50[107机场]`。
  - 其他前缀 `$4A/$02/$C1/$0A/$1E/$52/$51…` 区分选项类型(话题/对话/子菜单/跳转),**语义待逐个解**。
- **★「前往」= 菜单标签句 100**(在交谈块 `+15:[100=前往]`)。回答了核心疑问:**"前往"不是特殊
  机制,就是菜单里的一个选项**;选它 → 触发切场景(§一的 `$8307` 命令)。

### ★★菜单选项提取器已打通(核心机制验证)

选项在 `$04E8` 表的位置 = **`$0529[$21]`(当前层基址,累积)+ 光标×2**;`$0528` 是累积写入位。
读 `$04E8[base + i×2]` → 选项块地址 → 块头 2 字节 = 标签句号。递归各层 = 遍历图选项树。

**实测海边(逐层提取,句号全对)**:
- 顶层:`94/97/102` = 查看 / 交谈 / 回头看
- 查看 → `105/106` = 大海 / 山丘那边
- 交谈 → `99/96` = 梓 / 艾莉娜

工具雏形 `scratchpad/mnav.py`（动态导航各层读 `$04E8` 标签句）。**"直接从菜单数据提取选项树"已经能做到。**

### ★★★白盒遍历图提取器工作(`reversing/senmap/menutree.py`)

递归读 `$04E8` 各层选项 + 选中记 `$F071` 对话句,直接输出场景遍历图。实测海边:
```
查看 → 大海[梓「这海好吧」] / 山丘那边[艾莉娜「那不就是机场」]
交谈 → 梓 → 父母的事[忠「梓啊你还记得爸爸吗」=句187] ; 艾莉娜 → 父母的事[句?]
回头看 [忠「等一下」]
```
**句 187(爸爸戏)被白盒精确定位在「交谈→梓→父母的事」**——黑盒难找的深路径,引擎数据直接给出。
雏形待打磨:子菜单节点对话句混入标签句(需过滤)、探尽解锁的选项(机场/前往)需累积探尽、所有场景。

## ★★★★整章白盒遍历图达成(menutree.py 跑遍 7 场景)

`menutree.py` 对每个场景 checkpoint 提取选项树+对话句+前往边,**整章第一章遍历图直接从引擎
数据推导出来**(不玩游戏)。产物 `reversing/data/chapter_tree.txt`(含译文→gitignore)。样例:
- 海边:查看→大海/山丘 · 交谈→梓/艾莉娜→父母(句187) · 回头看
- 店内↔查米近景:**前往边 11↔17** · 工作间:阿源/车→驾驶舱、艾莉娜/阿源→机器人/查米的事
- 阿源车:梓/艾莉娜/窗户 · **休息→密语** · 密语屏:还是再叫醒一次/就这样乖乖休息

**回答了最初的问题:"能不能直接从代码/数据推导遍历图"——能,整章做到了。**

**打磨进展**:
- ✅ **垃圾句号根治**:根因=`$04E8` 一表两用(菜单选项表 ↔ 对话行指针表,`$A4D7` 排版复用)。
  用 **DOWN 测**(选中后按下看光标 `$2C` 动否:动=菜单递归 / 不动=对话叶子)区分,垃圾清零、
  场景号正确、话题叶子带对话句准确(阿源/车/大海/山丘等)。产物 `reversing/data/chapter_tree.txt`。
- ⚠ **深层递归的本质限制(与动态遍历同源)**:脚本流菜单里选项语义多样(话题/对话/子菜单/
  **导航类如"回头看"/切视角**/探尽解锁),单一判据难通吃。DOWN 测对"话题选项"准,但:
  ① "先播对话再开子菜单"的选项(梓→父母的事/句187)DOWN 测会判成叶子、漏其子菜单;
  ② 若强行"先推进对话再判",导航类选项("回头看")又被误当子菜单、递归展开出别处菜单。
  **这不是白盒特有,是脚本流选项语义多样的本质难点**(黑盒 traverse/digger 同样受限)。
  当前取"话题叶子准确"的稳定版;完整深层需给选项分类(话题/导航/深话题)更精细的判据——
  这是可继续打磨的方向,但骨架(选项树+话题对话+前往边)已准确、够用。
- 剩:与动态遍历 `audit.py` 72 句逐句交叉验证。

## ★★★★★纯静态选项解析器达成(`reversing/senmap/staticmenu.py`)

**动态 DOWN 测的病根**:靠"跑游戏按按钮、看光标动不动"**猜**选项类型,选项语义多样时猜错、漏项
(海边查看只探到大海/山丘,漏机场)。**正解 = 解菜单定义数据本身的语法**(数据是确定的,不用猜)。

**菜单定义语法(已破解)**:
- 菜单 = **顶层命令块链**(查看/交谈/回头看/休息…),块地址存 `$04E8` 指针表;块长 = 相邻指针差。
- 每块 = `标签句号(头2字节小端,Lua系)` + `内容 bytecode`。
- 内容里 **`$50 <标签句>` / `$02 <标签句>` = 一个子选项**(`$50`=看景/话题,`$02`=看人/导航);
  句100=前往(切场景)、句102=回头看;中间夹坐标命令(`$01/$05/$C1/$0A`)与对话 bytecode。
- 解析:按块边界切,块内扫 `$50/$02 <标签>`,**去重 + 剔块自引用 + 剔对话句(带「)+ 剔拉丁(职员表越界)**。

**实锤:纯静态 > 动态(处处更完整,一项不漏且不用猜)**:

| 场景 | 静态解析(staticmenu) | 动态 DOWN 测(menutree) | 静态多找出 |
|---|---|---|---|
| 0B 海边 | 查看→大海/艾莉娜/经销店/山丘/机场 | 查看→大海/山丘 | **机场/艾莉娜/经销店** |
| 0E 工作间 | 交谈→艾莉娜/阿源/机器人的事/查米的事/留言的事 | 交谈→艾莉娜/阿源→机器人/查米 | **留言的事**、查看多**到外面去** |
| 14 阿源车 | 交谈→艾莉娜/梓/兴奋雀跃 | 交谈→艾莉娜/梓 | **兴奋雀跃** |

**回答了"都读懂源码了还不能静态遍历出所有句子?"——能。** 选项层面纯静态、完整、确定。
现为"半静态"(动态读一次菜单定义字节,避开 `$5115` bank 映射定位);纯 ROM 定位需另解 bank 映射。

**剩余(选项→逐句)**:`$50 <标签>` 后跟的响应句、及"父母的事/留言的事"这类是否再开子块,
是块内下一层语法(响应句 / 子块指针),解开即可从"选项树"推进到"选项→完整对话"的纯静态逐句图。

## ★★★★★★对话演出引擎全解(选项→对话的完整机制,数据流实证)

用"数据流追踪(动态)+ 反汇编(静态,`reversing/tools/disasm6502.py`)"两手,把"选大海→句182"
从头到尾追通。**结论:选项块内容本身就是脚本,选中即把脚本指针指向它并跑解释器。**

### 1. 真正的对话演出解释器在 `$EDE5`(不是 `$8012`)

- 之前发现"菜单交互 `$8012` 命中 0 次"——因为对话演出走**另一个解释器 `$EDE5`**,脚本指针是
  **`$1E/$1F`**(bank 在 `$20`),不是 `$8012` 那套 `$C6/$C7`。这个 `$1E/$1F` 正是菜单填充选项表
  (`$83EA`)用的指针——**菜单与演出共用 `$1E/$1F`**。
- 主循环 `$EDDE`:`JSR $EDE5`(执行一条命令,进位标志控制)`; BCC 继续 ; BCS 重来`。
- `$EDE5` 一条命令:`LDA ($1E),Y` 读命令字节 → `(c>>5)&6` 选处理表 `$8004,X`(bankBD)→
  `c*4` 索引 → 读参数(标志位 `$66` 决定读几个参数到 `$5E-$61/$64/$65`)→ `$EE88`:`$1E += 命令长度` 前进。

### 2. 句号 → 句子指针 的公式(`$F01D` 解码器,完全破译)

```
$87/88 = 句号 × 3 + 基址表[$5D1E/$5D21, X]      (每句指针项3字节;X=句子库索引)
地址高3位 → bank(基址表 $5D1B,X)→ 写 $5115 ;  低位 → $A000 窗口地址
$F071: LDA ($87),Y → 读该句3字节(长度+文本地址)到 $80/$81/$82
```
- **句子库入口**:`$EF73`(库0:菜单标签)/`$EF96`(库1)/`$EFB9`(库2:剧情对话);句号从 X/Y 传入。
- **句子链式**:每句文本数据末尾带"下一句句号"(`$91C9` 读 `($29),Y→$12/$13`→作下一句号)→
  一句接一句自动播,直到结束标记(`$81==$1F && $87==$FE/$FF`,见 `$F061`)。

### 3. 选项→对话的完整链(大海实证,句号皆 Lua 系)

```
选中"大海"($88A0 选中逻辑, PC $88B5/$88BD, bank61)
  └→ $1E/$1F = $BF60 (大海选项块地址,从 $04E8 取)   ← 关键:脚本指针指向选项块
  └→ 跳过块头,$EDE5 解释器执行大海块内容 bytecode ($BF64 起, PC $EE8E 逐命令前进)
     └→ 块内命令执行到跳转 → bankA9:$B791 (大海演出脚本流)
        └→ 播 句181「梓「哥哥 这海好吧」忠「嗯」…」 → 链式续播
```

**这解释了为什么选项块里找不到 182**:选项块存的是**脚本命令**,对话在被执行的演出脚本流里。
`$8012 命中 0 次`也解释了:演出走 `$EDE5`/`$1E-1F`,与 `$8012`/`$C6-C7` 是两套并行解释器。

### 4. 通往纯静态反编译器(剩余)

**命令分派机制(完全破译)**:
```
命令字节 c → 类别 = (c>>5)&3 → 处理表基址 = $8004+类别*2 (bankBD; 4类别基址 $800C/$810C/$8168/$8268)
表项 = 基址[ (c*4)&0xFF ]:  +0=$13(属性) +1=$66(流内参数标志) +2/+3=处理程序地址→$1A/$1B
分派: JMP ($1A)  (每命令跳独立处理程序,可在任意 bank)
流内参数长度: $66&$08→读1字节到$64, $66&$80→再读1到$65 ; 但处理程序可再自读$1E(如$4E读2字节句号)
```
- **`$4E <句号lo> <句号hi>` = 显示对话**(处理程序取句子显示;实证 `4E B5 00`→句181 大海对话)。
- **库切换命令**(处理程序 `$EECE`):从脚本流 `$A000-$A00B`(12字节库描述符=3库×bank/lo/hi/…)
  重载 `$5D1B/$5D1E/$5D21` 三张基址表 → 这就是演出中"换句子库(库0/1/2)"的机制。
- `$02 <句号>`=看人选项、`$01/$05/$C1`=布局/坐标。

**命令长度表**(部分命令处理程序自读 `$1E`,长度非纯 `$66` 决定)→ 动态 trace 逐命令测步长补全:
`$01=2 $02=3 $03=2 $05=2 $1D=1 $27=1 $3C=3 $40=2 $41=1 $49=2 $4E=3 $00=2`(其余用 `$66` 公式)。

## ★★★★★★★纯静态演出脚本反汇编器达成(`reversing/senmap/scriptdis.py`)

**演出脚本流格式**(线性排列,每条对话 6 字节):
```
$40 00        开对话框
$4E <lo> <hi>  显示句号(小端)
$41           收尾
```
`scriptdis.py` 用"命令表 `$66` 公式 + trace/结构修正的长度表"静态扫描脚本 bytecode,遇 `$4E` 提句号。

**实证:纯静态提出大海演出全序列**(bankA9:`$B791`,无需跑游戏)——
```
$B793 4E B5 00 → 句181 梓「哥哥这海好吧」    $B7B7 4E BB 00 → 句187 梓「阿母的事全没了」★
$B799 4E B6 00 → 句182 梓「大海虽喜欢…」     $B7BD 4E BC 00 → 句188 忠「艾莉娜的父母…」
$B79F 4E B7 00 → 句183 艾莉娜「不要走神」    …连续到 句193+
```
**句号连续 181-193+,与动态 trace 完全一致**;硬骨头句187 静态一扫即得(不再需动态深挖)。

**这就是"能不能直接从代码推导遍历图/对话"的终极肯定回答:能——连对话内容都纯静态从 bytecode
反汇编出来了。** 演出引擎从"黑箱"到"逐字节可读":解释器、句号公式、命令分派、对话命令、脚本流格式全通。

### 控制流跳转 `C1`(选项块→演出脚本入口,机制已破)

`C1 <lo> <hi>` 命令处理(`$F38F`):`LDX $64; LDY $65`(读的2字节)→ `JSR $EF73`(**当作库0"句号"取其
文本地址**)→ `STX $1E; STY $1F; STA $20`(把该地址设为新脚本指针)。**惊人的统一:演出脚本本身就
存在句子库里当"句子",C1 用"句号"经 `$F01D` 公式定位演出脚本入口** —— 脚本与对话共用一套寻址。

`$F01D` 公式(静态可复现):`t = 句号×3 + (base_lo|base_hi<<8)`;句子指针 `addr = $A000|(t&$1FFF)`,
`bank = ((t>>13)&7)+base_bank`。base 三张表 `$5D1B/$5D1E/$5D21`(按库 0/1/2 索引)。

**★全静态链已验证打通(海边"看"演出)**:
```
C1 库0句号 94  →$F01D(海边base 80/8C/00)→ 句子指针 bank80:$A1A6
              →读3字节 = [29 91 17]
              →$EF73解读: bank=$29|$80=A9, addr=$91|($17+$A0)<<8=$B791
              → 演出脚本入口 bankA9:$B791  ✓ 与运行时完全一致
```
即"句子指针表"的条目本身就是"演出脚本地址"——库0是脚本入口表。**base 表实测在演出中零写入**
(场景初始化时设定、演出中稳定),故只需每场景动态读一次 `$5D1B-$5D23`,C1 跳转即可纯静态跟随:
选项块 → `C1 库0句号` →(`$F01D`+读指针3字节)→ 演出脚本地址 → 扫描 `$4E` 提对话。**全自动链闭合。**

### ★★全自动闭环工具 `reversing/senmap/autoscript.py`

把整条链固化:`库0句号 → $F01D → 句子指针 → 读3字节 → 演出脚本入口 → scriptdis 扫描 → 对话句号序列`。
实测 `autoscript.py 94`(海边"看"演出)纯静态提出句181-190+(含硬骨头句187),与运行时一致。
海边"看/谈"是**一条连续演出流**(句181起),各选项(查看94/交谈97/梓99/山丘106…)的库0句号
指向**同一流的不同入口偏移**(B791/B7A3/B7AF/B7C7…);个别选项(大海105/父母108)→bankA1 另一段流。
**从"能不能"到"能,且已工具化":给库0句号+场景base,纯静态吐出完整对话,不跑游戏。**
(剩:各选项→其库0演出句号的最外层映射(父选项=标签句号已验证,子选项待理清)、bankA1 段命令适配。)

### ★交叉验证:静态=全集 ⊇ 动态=路径子集(脚本流的终极印证)

静态 `autoscript 94` 提出海边"看/谈"剧情段 **句181-216 全集**;而动态遍历(`ch1_sentence_scene`)
在同段只碰到 **181,182,184,185,186,188,189**(跳着的)。**动态是静态的路径子集** ——
演出区是句子按号线性存储的数据,静态一次给全集,动态只走到运行时路径碰到的那些。

**这是"暴力遍历会不会漏"的终极解答**:早期用动态穷尽(seqrun×traverse×audit)去逼近完整,始终
有"路径没走到=漏句"的隐患;**静态反编译直接从数据区给全集,天然不漏**。至此完整遍历的正解清晰:
**静态反编译出句子全集(不漏)× 动态遍历定实际路径/画面(验证)** = 可证完整,两法各补对方的盲区。

### 全章静态转储的最后一环(认知已清晰)

- **base 表全局固定**(实测 0B 与 0E 相同 `80 80 81 8C B5 4C 00 1C 1D`)→ 库0句号→演出地址是
  **全局映射**;库0句号94→B791 是"海边看海"专属,各场景的演出是**不同库0句号**指向的不同段。
- **各场景演出入口句号 = 选中时 `$88A0` 逻辑设入 `$64` 的值**(海边"看"→94;工作间"看"→另一句号)。
  这个"选项→其库0演出句号"是最外层一环,**动态可抓**(选中时读 `$64`)、静态需再解 `$88A0` 一段。
- **全章转储 = 各场景动态抓入口句号 `$64` × `autoscript` 静态展开该段**(即"静态全集×动态路径"的落地)。
  展开机制与工具链(`autoscript`/`scandump`)全部就绪,只差这一环的枚举——方法已完整验证。

## 进度小结

- ✅ 引擎架构(两套系统)、过场脚本命令集、对话菜单机制、菜单定义格式、选项提取、**整章遍历图提取**——全打通
- 白盒反编译从"可行性"一路做到"**整章遍历图产出**";剩的是边界打磨 + 交叉验证 + (可选)纯静态化。

## 剩余工作(通往完整反编译器)

1. 对话菜单系统:选项表结构、选项→对话/跳转的映射、「前往」如何从菜单触发 `$8012` 切场景
2. 条件分支命令(剧情推进条件)
3. 完整命令集剩余语义(`$03-$0F`、`$90-$B3` 等)
4. 脚本入口地址(第一章从哪条 bytecode 起)
5. **反编译器**:从入口顺序解释、遇菜单展开分支、遇跳转跟随 → 输出遍历图 + 逐句
6. 与动态遍历(`audit.py` 的 72 句)交叉验证

**规模诚实说明**:这是 FC 脚本引擎的完整逆向 + 反编译器,是长期工程(几十轮量级),
非短期可完结。当前已确立**可行性**(bytecode 可解析、切场景/跳转命令已定位)。
第一章的"完美遍历"目前由动态方案(`seqrun × traverse × audit`)达成并 PASS,不阻塞于此。
